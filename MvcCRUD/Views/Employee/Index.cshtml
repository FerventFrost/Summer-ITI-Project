@model List<Employee>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<a href="/Employee/Create" class="btn btn-primary">Create New</a>

<br />
<br />
<br />

<table class="table table-bordered table-striped table-hover">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Age</th>
        <th>Department</th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td>@item.Age</td>
            <td>@item.DeptId</td>
            <td>
                <a href="/Employee/Edit/@item.Id" class="btn btn-success">Edit</a> |
                <a href="/Employee/Details/@item.Id" class="btn btn-secondary">Details</a> |
                <a href="/Employee/Delete/@item.Id" class="btn btn-danger" onclick="return confirm('Are You Sure Delete @item.Name');">Delete</a> 
            </td>
        </tr>
    }
</table>